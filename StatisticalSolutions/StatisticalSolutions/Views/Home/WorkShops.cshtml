@using System.Collections.Generic;
@using StatisticalSolutions.Models;
@model List<seminar>
@{
    ViewBag.Title = "WorkShops";
}


<div class="container focus">
    <h4>Currently Available WorkShops</h4>

</div>


<div class='container twelve.columns'>

    @foreach (seminar sem in Model)
    {
        <section class='gray'>
            <h4>
                <strong>
                    @*<a  name="Basics" id="Basics"></a>*@
                    @Html.Raw(sem.TitleHtml)
                </strong>
            </h4>
            <br />
            <div>
                @Html.Raw(sem.EventDetailsHtml)
            </div>
            <p>
                Available dates : <b>
                    @if (sem.StartDate != null && sem.StartDate != default(DateTime))
                    {
                        @Html.Raw(sem.StartDate.ToShortDateString())
                    }
                </b>
            </p>
            <div>

                @if (sem.StartDate != null && sem.StartDate != default(DateTime))
                {
                    registration reg = new registration();
                    reg.seminar_id = sem.seminar_id;
                    <a href='@Url.Action("Register", "Home", reg)' class='button medium green'>Register Now</a>
                }


            </div>
        </section>

    }


</div>