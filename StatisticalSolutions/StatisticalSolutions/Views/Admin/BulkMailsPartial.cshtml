@using StatisticalSolutions.Models
@model BulkMailModel
<link href="~/Content/bootstrap.css" rel="stylesheet" />

    @if (Model.Students.Any())
    {
    <div style="color:red;"> @Model.Students.Count active registered students found for this seminar </div>
        
       <div>
           @if (Model.Registration != null)
           {
          
           @Html.HiddenFor(model => model.Registration.seminar_id)
         

           }
   </div>
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th style="width:30%;">Name</th>
                <th style="width:40%;">Email</th>
                <th style="width:20%;">Contact</th>
                <th style="width:10%;">Select</th>
            </tr>

        </thead>
        <tbody>
            @foreach (student student in Model.Students)
            {
                @Html.Partial("_BulkMailTableContentPartial", student)
            }

        </tbody>
    </table>
    
        <input type="submit" tabindex="3" onclick="sendMails()" value="Send Mails" class="button medium green filter-button right-align" />

    }